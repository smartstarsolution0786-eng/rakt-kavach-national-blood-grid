{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "start": "node artifacts/api-server/index.js",
    "build:cloudflare": "pnpm --config.shamefully-hoist=true install && pnpm -r --if-present run build"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.4",
    "typescript": "~5.9.3"
  }
}
